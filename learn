数据存储的最小单位是节（Byte）
1节（Byte）=8位（bit）

位（bit）一个数字0 或1 代表一位









数据存储的最小单位是 节（Byte）
1节（Byte）=8 位（bit）

位（bit）:一个数字0 或1 代表 一位









位（bit）:一个数字0 或 1 代表一位
8bit=1Byte
8位=1字节




























